package bssic;

public class Simple {
	
	public static void main(String args[]) {
		System.out.println("Strat to  Learn JAVA");
	}
//Variables
	//Local variable -inside the body we can;t call any whare
	//instance variable -inside the class out side the body
	//static variable- A variable that is declared as static is called 
	//a static variable. It cannot be local. You can create a
	//single copy of the static variable and share it among all the instances of the class.
	//Memory allocation for static variables happens only once when the class is loaded in the memory.

	
	//
}
